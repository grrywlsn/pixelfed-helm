apiVersion: v2
name: pixelfed
version: v2.0.7
description: Helm chart to deploy Pixelfed
appVersion: 0.12.5
dependencies:
  - name: redis
    version: "21.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.internal
